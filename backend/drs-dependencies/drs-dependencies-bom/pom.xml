<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- BOM 独立声明，不带父引用，避免与聚合父 pom 形成导入循环 -->
    <groupId>cn.edu.fzu.drs</groupId>
    <artifactId>drs-dependencies-bom</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>drs-dependencies-bom</name>
    <description>第三方依赖版本声明（BOM）</description>

    <properties>
        <mybatis-plus.version>3.5.11</mybatis-plus.version>
        <knife4j.version>4.5.0</knife4j.version>
        <jedis.version>5.2.0</jedis.version>
        <minio.version>8.5.7</minio.version>
        <swagger-annotations.version>2.2.8</swagger-annotations.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- 自 3.5.9 起 PaginationInnerInterceptor 迁移至此独立模块，官方 starter 不再传递引入 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.xiaoymin</groupId>
                <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
                <version>${knife4j.version}</version>
            </dependency>
            <dependency>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-annotations-jakarta</artifactId>
                <version>${swagger-annotations.version}</version>
            </dependency>
            <dependency>
                <groupId>redis.clients</groupId>
                <artifactId>jedis</artifactId>
                <version>${jedis.version}</version>
            </dependency>
            <!-- MinIO 对象存储 Java 客户端（S3 兼容），用于眼底图与热力图存储 -->
            <dependency>
                <groupId>io.minio</groupId>
                <artifactId>minio</artifactId>
                <version>${minio.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
</project>
